#include "parsers/validations.h"
#include <stdio.h>


bool check_artist(Catalog *manager, char **tokens, unsigned n_tokens) {
    if (manager == NULL || tokens == NULL)
        return false;

    

    return false;
}